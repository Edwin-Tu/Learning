# 🧠 C/C++ 知識整理筆記

本文件整理了 C/C++ 中常見且基礎的重要觀念，包含：
- `#include` 導入的資料結構與功能  
- 資料型態宣告方式  
- `using namespace` 的意義  
- `for` 與 `while` 迴圈差異與使用時機  

---

## 📁 1. #include 導入的資料結構與功能

`#include` 是 **前置處理器指令 (Preprocessor Directive)**，在編譯前會將指定檔案內容插入程式中。

### 常見標頭檔
| 標頭檔 | 功能簡述 |
|--------|----------|
| `<iostream>` | 輸入輸出（`cin`, `cout`） |
| `<stdio.h>` | C 標準輸入輸出（`printf`, `scanf`） |
| `<string>` | C++ 字串類別與相關函式 |
| `<vector>` | 動態陣列容器 |
| `<algorithm>` | 常用演算法（`sort`, `find`, `max`, `min`） |
| `<cmath>` | 數學函式（`pow`, `sqrt`, `sin`, `cos`） |
| `<cstdlib>` | 通用函式（`rand`, `malloc`, `free`） |

### 範例
```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    std::vector<int> nums = {5, 3, 9, 1};
    std::sort(nums.begin(), nums.end());
    for (int n : nums) std::cout << n << " ";
    return 0;
}
```

---

## 🔤 2. 資料型態的宣告

### 基本資料型態
| 型態 | 範例 | 說明 |
|------|------|------|
| `int` | `int age = 20;` | 整數 |
| `float` | `float height = 170.5;` | 單精度浮點數 |
| `double` | `double pi = 3.14159;` | 雙精度浮點數 |
| `char` | `char grade = 'A';` | 字元 |
| `bool` | `bool isOpen = true;` | 布林值 |
| `string` (C++) | `string name = "Amber";` | 字串（需 `#include <string>`） |

### 陣列與指標
```cpp
int scores[3] = {80, 90, 100};
int* ptr = scores; // 指向陣列開頭
```

---

## 🌐 3. using namespace 的意義

在 C++ 中，標準函式庫（如 `cout`, `vector`）都屬於 `std` 命名空間。

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Hello, world!" << endl;
}
```

若不使用 `using namespace std;`，則需完整指定：
```cpp
std::cout << "Hello, world!" << std::endl;
```

> ⚠️ **建議做法**：在大型專案中不要直接 `using namespace std;`，以避免命名衝突。  
> 可改為只導入特定名稱：
> ```cpp
> using std::cout;
> using std::endl;
> ```

---

## 🔁 4. for 與 while 迴圈

### for 迴圈
適合 **已知次數** 的重複執行。
```cpp
for (int i = 0; i < 5; i++) {
    cout << i << " ";
}
```

### while 迴圈
適合 **未知次數**、依條件控制的情境。
```cpp
int i = 0;
while (i < 5) {
    cout << i << " ";
    i++;
}
```

### do-while 迴圈
至少執行一次的版本。
```cpp
int i = 0;
do {
    cout << i << " ";
    i++;
} while (i < 5);
```

---

## 🧩 小結

| 主題 | 重點摘要 |
|------|-----------|
| `#include` | 導入標頭檔以使用外部功能 |
| 資料型態 | 定義變數類型以儲存不同資料 |
| `using namespace` | 命名空間管理，減少名稱衝突 |
| 迴圈 | 控制流程以重複執行指令 |

---

📘 **建議延伸學習：**
- 指標與參考（Pointer & Reference）
- 結構與類別（Struct & Class）
- STL 容器（`map`, `set`, `queue`, `stack`）
- 函式與遞迴（Function & Recursion）


# 🧠 C/C++ 知識整理筆記

本文件整理了 C/C++ 中常見且基礎的重要觀念，包含：
- `#include` 導入的資料結構與功能  
- 資料型態宣告方式  
- `using namespace` 的意義  
- `for` 與 `while` 迴圈差異與使用時機  

---

## 📁 1. #include 導入的資料結構與功能

`#include` 是 **前置處理器指令 (Preprocessor Directive)**，在編譯前會將指定檔案內容插入程式中。

### 常見標頭檔
| 標頭檔 | 功能簡述 |
|--------|----------|
| `<iostream>` | 輸入輸出（`cin`, `cout`） |
| `<stdio.h>` | C 標準輸入輸出（`printf`, `scanf`） |
| `<string>` | C++ 字串類別與相關函式 |
| `<vector>` | 動態陣列容器 |
| `<algorithm>` | 常用演算法（`sort`, `find`, `max`, `min`） |
| `<cmath>` | 數學函式（`pow`, `sqrt`, `sin`, `cos`） |
| `<cstdlib>` | 通用函式（`rand`, `malloc`, `free`） |

### 範例
```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    std::vector<int> nums = {5, 3, 9, 1};
    std::sort(nums.begin(), nums.end());
    for (int n : nums) std::cout << n << " ";
    return 0;
}
```

---

## 🔤 2. 資料型態的宣告

### 基本資料型態
| 型態 | 範例 | 說明 |
|------|------|------|
| `int` | `int age = 20;` | 整數 |
| `float` | `float height = 170.5;` | 單精度浮點數 |
| `double` | `double pi = 3.14159;` | 雙精度浮點數 |
| `char` | `char grade = 'A';` | 字元 |
| `bool` | `bool isOpen = true;` | 布林值 |
| `string` (C++) | `string name = "Amber";` | 字串（需 `#include <string>`） |

### 陣列與指標
```cpp
int scores[3] = {80, 90, 100};
int* ptr = scores; // 指向陣列開頭
```

---

## 🌐 3. using namespace 的意義

在 C++ 中，標準函式庫（如 `cout`, `vector`）都屬於 `std` 命名空間。

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Hello, world!" << endl;
}
```

若不使用 `using namespace std;`，則需完整指定：
```cpp
std::cout << "Hello, world!" << std::endl;
```

> ⚠️ **建議做法**：在大型專案中不要直接 `using namespace std;`，以避免命名衝突。  
> 可改為只導入特定名稱：
> ```cpp
> using std::cout;
> using std::endl;
> ```

---

## 🔁 4. for 與 while 迴圈

### for 迴圈
適合 **已知次數** 的重複執行。
```cpp
for (int i = 0; i < 5; i++) {
    cout << i << " ";
}
```

### while 迴圈
適合 **未知次數**、依條件控制的情境。
```cpp
int i = 0;
while (i < 5) {
    cout << i << " ";
    i++;
}
```

### do-while 迴圈
至少執行一次的版本。
```cpp
int i = 0;
do {
    cout << i << " ";
    i++;
} while (i < 5);
```

---

## 🧩 小結

| 主題 | 重點摘要 |
|------|-----------|
| `#include` | 導入標頭檔以使用外部功能 |
| 資料型態 | 定義變數類型以儲存不同資料 |
| `using namespace` | 命名空間管理，減少名稱衝突 |
| 迴圈 | 控制流程以重複執行指令 |

---

📘 **建議延伸學習：**
- 指標與參考（Pointer & Reference）
- 結構與類別（Struct & Class）
- STL 容器（`map`, `set`, `queue`, `stack`）
- 函式與遞迴（Function & Recursion）
